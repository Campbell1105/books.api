function checkCoupon(enteredCode, correctCode, currentDate, expireDate){

const enteredCode = c1
const correctCode = c2


}
const currentDate = d1
const expireDate = d2
function areSameDate(d1, d2) {
    return d1.getFullYear() >= d2.getFullYear()
        && d1.getMonth() >= d2.getMonth()
        && d1.getDate() >= d2.getDate();
}